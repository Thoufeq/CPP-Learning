#include <bits/stdc++.h>
using namespace std;

int main(){
    // Compiletime Error(The code doesnt get compiled(no binary conversion))
    // Causes of compile-time error - syntax, declaration, type and header error.
    cout << "Hello" << endl // Semicolon is missing
    
    
    //Runtime Error(The code compiles into binary code but it won't run.)
    cout << 7/0; // warning: division by zero [-Wdiv-by-zero]

    return 0;
}