#include <bits/stdc++.h>
using namespace std;

int main() {
    cout << "Hello World!" << endl;
    return 0; // The return 0; at the end of a C++ main() function 
              // is a signal from your program to the Operating System (OS). 
              // Since main() is defined with an int return type, 
              // it must return an integer once it finishes its job.
}